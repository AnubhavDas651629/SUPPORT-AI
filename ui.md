knowledge base ->
Redesign the Support-AI Knowledge Base page.

The goal is to make the RAG pipeline understandable without turning the page into a technical diagram.

The user should be able to understand:

Document
→ ingestion
→ chunking
→ embeddings
→ pgvector
→ retrieval

The page should prioritize:

1. Knowledge base overview
2. Documents
3. Indexing status
4. Upload/ingestion
5. Retrieval testing

Avoid turning each section into a large rounded card.

Use structured sections, tables, status indicators, and subtle dividers.

Make document processing states visually clear:
- processing
- indexed
- failed

The retrieval simulator should feel like a real developer/operator tool rather than a decorative demo.

Do not add fake functionality.
Do not change backend behavior.


AI Assistant / Agent page->
Redesign the Support-AI AI Agent configuration page.

This should feel like a serious AI agent configuration console.

Organize the page around:

IDENTITY
- agent name
- greeting
- appearance

BEHAVIOR
- system instructions
- model
- response behavior

KNOWLEDGE
- connected knowledge bases
- retrieval configuration

DEPLOYMENT
- widget
- deployment status

Keep a live preview on the right if useful.

The design should resemble a professional developer/product configuration tool rather than an AI marketing page.

Use restrained colors.

Avoid:
- glowing AI effects
- animated backgrounds
- excessive gradients
- decorative robot graphics
- excessive pills

The AI should feel powerful because of the information and controls available, not because of visual effects.